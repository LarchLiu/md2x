services:
  md2x-mcp:
    build: .
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    environment:
      - PORT=${PORT:-3000}
      - MD2X_BASE_URL=${MD2X_BASE_URL:-http://localhost:3000}
    volumes:
      - md2x-resources:/app/resources
    restart: unless-stopped

volumes:
  md2x-resources:
